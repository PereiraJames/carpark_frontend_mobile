/** Shared design tokens for the floating map overlays (search, controls, sheets, etc). */

export const COLORS = {
  primary: '#2a81cb',
  text: '#1c1c1e',
  muted: '#7a7a7a',
  surface: '#ffffff',
  danger: '#d64545',
};

export const RADIUS = {
  sm: 10,
  md: 14,
  lg: 22,
};

export const CARD_SHADOW = {
  elevation: 4,
  shadowColor: '#000',
  shadowOpacity: 0.12,
  shadowRadius: 8,
  shadowOffset: { width: 0, height: 2 },
};

/** Shared size for the square/circular floating buttons (toggle, locate, search). */
export const BUTTON_SIZE = 46;
export const BUTTON_GAP = 8;
